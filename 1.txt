변수
    항상 const, 가끔 let(업데이트 가능), 절대 var

배열
    const 배열명 = [, , , ,] 다양한 자료형이 들어갈 수 있다.
    배열 속에 데이터추가
        배열명.push();

오브젝트
    Create, Update 가능
const player = {
    name: "asdasd",
    ...,
    ...
};
console.log(player.name);
오브젝트.property(소유물)
언제든 오브젝트 안의 값을 변경(Update)할 수 있고, 추가 또한 가능하다

() --> function을 실행하는 것
오브젝트 안에서의 function지정은 아래처럼 해준다.
sayHello: function(){
        
}

#조건문 
    if, else if, else
        false, undefined, null, 0, NaN, the empty string("")을 거짓으로 취급함
    swich
        똑같아. 마지막에 default만 잘 넣어줘
    삼항연산자
        조건 ? true실행 : false실행;

#기본함수
    prompt() 안씀;
        사용자에게 창을 띄울 수 있도록 해줌
        입력받은 값은 string임
    parseInt()
        인트형 변환
        C#에서 int.Parse();
    isNaN()
        boolean으로 알려줌

#typeof
    타입을 볼 수 있음

#null과 undefined의 차이
    null
        존재하고 값이 있는데 그게 아무것도 아니다
    undefined
        존재하지만, 안에 아무것도 없다

JavaScript파일에서 HTML element를 가지고 올 수 있다
    하나의 ex) in the HTML <h1 id="title">Grab me!</h1>
    const title = document.getElementById("title");
    수정도 가능하다
    title.innerText = "Got you!";
    읽기도 가능
    console.log(title.className);

    가져오는 Hot한 방법 document.querySelector, querySelectorAll
    #querySelector
        element를 CSS방식으로 검색할 수 있음
        단 1개의 element를 찾음
    #querySelectorAll
        Array형식으로 다 나옴

#Events
    